---
phase: 01-green-buddy-ux
plan: 03
type: execute
---

<objective>
Create the image-analyzer microservice using Claude Vision API to analyze photos of meals, groceries, and clothing.

Purpose: Core capability for day-by-day tracking — users take photos and the system extracts items + estimates CO2 impact.
Output: New `image-analyzer` service with endpoints for meal, grocery, and clothing photo analysis.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@ai-narrative/app/service.py
@ai-narrative/app/main.py
@ai-narrative/Dockerfile
@docker-compose.yml
@footprint-engine/app/factors.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold image-analyzer service</name>
  <files>image-analyzer/app/main.py, image-analyzer/app/config.py, image-analyzer/app/router.py, image-analyzer/app/schemas.py, image-analyzer/Dockerfile, image-analyzer/requirements.txt, docker-compose.yml</files>
  <action>
  Create a new microservice following the exact same structure as ai-narrative:

  **image-analyzer/app/main.py:** FastAPI app with lifespan, health endpoint, router registration. Port 8008.

  **image-analyzer/app/config.py:** Pydantic BaseSettings with ANTHROPIC_API_KEY, MODEL_NAME (default claude-sonnet-4-20250514), LOG_LEVEL.

  **image-analyzer/app/schemas.py:**
  - ImageAnalysisRequest: image_base64 (str), analysis_type (enum: meal, grocery, clothing), user_description (optional str — text alternative to photo)
  - MealAnalysisResult: items (list of {name, portion, estimated_co2_kg}), total_co2_kg, confidence (low/medium/high)
  - GroceryAnalysisResult: items (list of {name, quantity, category, estimated_co2_kg}), total_co2_kg, confidence
  - ClothingAnalysisResult: items (list of {type, material, brand_guess, estimated_co2_kg}), total_co2_kg, confidence
  - ImageAnalysisResponse: analysis_type, result (union of above), analyzed_at, is_fallback

  **image-analyzer/app/router.py:** POST /api/v1/analyze endpoint accepting ImageAnalysisRequest.

  **Dockerfile:** Copy from ai-narrative pattern, port 8008.

  **docker-compose.yml:** Add image-analyzer service on port 8008, ecosignal-net network, depends_on redis.

  **requirements.txt:** fastapi, uvicorn, anthropic, pydantic, pydantic-settings
  </action>
  <verify>cd image-analyzer && python -c "from app.schemas import ImageAnalysisRequest, MealAnalysisResult; print('OK')"</verify>
  <done>image-analyzer service scaffolded with all schemas, Dockerfile, and docker-compose entry.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Claude Vision analysis logic</name>
  <files>image-analyzer/app/service.py, image-analyzer/app/prompts.py</files>
  <action>
  **image-analyzer/app/prompts.py:**
  Create 3 system prompts (Italian-aware, JSON output):

  MEAL_PROMPT: "Analizza questa foto di un pasto. Identifica ogni alimento, stima la porzione in grammi, e calcola l'impatto CO2 in kg basandoti su questi fattori medi:
  - Carne rossa: 0.027 kg CO2/g
  - Pollo: 0.0069 kg CO2/g
  - Pesce: 0.006 kg CO2/g
  - Latticini: 0.0033 kg CO2/g
  - Verdure: 0.002 kg CO2/g
  - Cereali/pasta: 0.0014 kg CO2/g
  - Frutta: 0.001 kg CO2/g
  Rispondi SOLO in JSON: {items: [{name, portion_g, estimated_co2_kg}], total_co2_kg, confidence}"

  GROCERY_PROMPT: Similar for grocery items (packaging, transport factors).

  CLOTHING_PROMPT: Similar for clothing (material lifecycle: cotton=8kg/item, polyester=12kg, wool=10kg, linen=5kg).

  **image-analyzer/app/service.py:**
  - async def analyze_image(request: ImageAnalysisRequest, client: AsyncAnthropic) → ImageAnalysisResponse
  - Use anthropic SDK with vision: pass image as base64 content block
  - If user_description provided (no photo), use text-only analysis
  - Parse JSON response, handle markdown code blocks
  - Fallback: return is_fallback=True with empty items on error
  - Use tenacity retry (3 attempts, exponential backoff) like ai-narrative
  </action>
  <verify>cd image-analyzer && python -c "from app.service import analyze_image; from app.prompts import MEAL_PROMPT; print('OK')"</verify>
  <done>Vision analysis works for meal, grocery, and clothing. Handles both photo (base64) and text description inputs. Has fallback on error.</done>
</task>

<task type="auto">
  <name>Task 3: Add image analysis routes to API gateway</name>
  <files>api-gateway/app/router.py, api-gateway/app/schemas.py</files>
  <action>
  Add to api-gateway/app/router.py:
  - New router group: image_router with prefix /image
  - POST /api/v1/image/analyze → proxy to image-analyzer:8008/api/v1/analyze
  - Requires JWT auth (inject user_id)
  - Rate limit: 20/minute (users will send photos frequently but not spam)

  Add to api-gateway/app/schemas.py any mirror schemas needed for validation.

  The gateway proxies the base64 image + analysis_type to the image-analyzer service and returns the response.
  </action>
  <verify>Review api-gateway/app/router.py for image_router registration</verify>
  <done>API gateway has /api/v1/image/analyze endpoint proxying to image-analyzer service.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] image-analyzer service builds in Docker
- [ ] POST /api/v1/image/analyze accepts base64 image + type
- [ ] Claude Vision API called with correct prompt per analysis type
- [ ] Text description fallback works (no photo needed)
- [ ] API gateway proxies requests correctly
</verification>

<success_criteria>

- All tasks completed
- image-analyzer service fully functional
- 3 analysis types supported (meal, grocery, clothing)
- Both photo and text-description inputs work
- Integrated into API gateway with auth
</success_criteria>

<output>
After completion, create `.planning/phases/01-green-buddy-ux/01-03-SUMMARY.md`
</output>
