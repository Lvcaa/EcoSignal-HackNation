---
phase: 01-green-buddy-ux
plan: 02
type: execute
---

<objective>
Expand the frontend onboarding flow with new steps and display baseline CO2 after completion.

Purpose: Users need to provide commute frequency, pet info, and home details during onboarding. After completing onboarding, they should see their baseline CO2 score.
Output: New onboarding steps (CommutStep, PetStep, HomeStep), auto-trigger footprint calculation, baseline score display.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@frontend/src/pages/onboarding/ZipStep.jsx
@frontend/src/pages/onboarding/TransportStep.jsx
@frontend/src/pages/onboarding/DietStep.jsx
@frontend/src/components/onboarding/OnboardingShell.jsx
@frontend/src/components/onboarding/ProgressDots.jsx
@frontend/src/api/profile.js
@frontend/src/api/footprint.js
@frontend/src/App.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create new onboarding step components</name>
  <files>frontend/src/pages/onboarding/CommuteStep.jsx, frontend/src/pages/onboarding/HomeStep.jsx, frontend/src/pages/onboarding/PetStep.jsx</files>
  <action>
  Create 3 new onboarding step pages following the same pattern as ZipStep.jsx/TransportStep.jsx/DietStep.jsx (SelectionCard-based UI):

  **CommuteStep.jsx:**
  - Question: "Quanti giorni alla settimana vai al lavoro?"
  - Options: 0-7 days as SelectionCards with icons (🏠 0 = Smart working, 1-2 = Occasionale, 3-4 = Part-time, 5 = Full-time, 6-7 = Intenso)
  - Stores commute_days_per_week in onboarding state

  **HomeStep.jsx:**
  - Combines home_type (apartment/house) selection AND home_size_sqm slider/input (10-300 m²)
  - Currently these are NOT in any onboarding step — they exist in the schema but were likely meant for a later step
  - Use SelectionCards for type + a range slider for size

  **PetStep.jsx:**
  - Question: "Hai animali domestici?"
  - First: Yes/No toggle (has_pets)
  - If Yes: pet_type selector (🐕 Cane, 🐈 Gatto, 🐹 Piccolo animale) + pet_count (1-5 number selector)
  - If No: skip to next

  Follow existing UI patterns: TailwindCSS classes, SelectionCard component, same color palette. All text in Italian.
  </action>
  <verify>Files exist and have valid JSX syntax</verify>
  <done>3 new step components created with Italian UI text, matching existing design system.</done>
</task>

<task type="auto">
  <name>Task 2: Wire new steps into onboarding flow and trigger baseline calculation</name>
  <files>frontend/src/components/onboarding/OnboardingShell.jsx, frontend/src/components/onboarding/ProgressDots.jsx, frontend/src/App.jsx, frontend/src/api/profile.js, frontend/src/api/footprint.js, frontend/src/pages/onboarding/DietStep.jsx</files>
  <action>
  Update OnboardingShell.jsx:
  - Add new steps to the flow: Zip → Transport → Commute → Diet → Home → Pet
  - Update ProgressDots to show 6 steps instead of 3
  - On final step (Pet) completion: POST /api/v1/profile/onboarding with ALL fields, then immediately call POST /api/v1/footprint/calculate
  - Store the baseline footprint result in Zustand or pass it to the dashboard

  Update App.jsx routing if needed to include new step routes.

  Update DietStep.jsx: it currently triggers the onboarding POST on completion. Change it to just advance to HomeStep instead.

  The last step (PetStep) should:
  1. Call profile onboarding API with all collected data
  2. Call footprint calculation API
  3. Navigate to dashboard with baseline score available
  </action>
  <verify>Onboarding flow renders all 6 steps in sequence when navigating forward</verify>
  <done>Onboarding has 6 steps. Final step triggers both profile save and footprint calculation. User lands on dashboard with baseline CO2.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Extended onboarding flow with 6 steps (Zip, Transport, Commute, Diet, Home, Pet) that calculates baseline CO2</what-built>
  <how-to-verify>
    1. Run: docker compose up --build
    2. Visit: http://localhost:3000
    3. Register a new account
    4. Walk through all 6 onboarding steps:
       - ZIP code selection
       - Transport mode
       - Commute days (0-7)
       - Diet type
       - Home type + size
       - Pets (yes/no + details)
    5. Confirm: After final step, redirected to dashboard
    6. Confirm: Dashboard shows a baseline CO2 score
    7. Check: Progress dots show 6 steps correctly
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All 6 onboarding steps render correctly
- [ ] Profile saved with all new fields
- [ ] Footprint calculated with pet and commute data
- [ ] Baseline CO2 visible on dashboard
- [ ] No console errors
</verification>

<success_criteria>

- All tasks completed
- Full onboarding flow works end-to-end
- Baseline CO2 score displayed after onboarding
- UI is in Italian, matches existing design system
</success_criteria>

<output>
After completion, create `.planning/phases/01-green-buddy-ux/01-02-SUMMARY.md`
</output>
