---
phase: 01-green-buddy-ux
plan: 05
type: execute
---

<objective>
Implement weekly appliance surveys (lavatrice/lavastoviglie) with scheduler-triggered reminders and CO2 integration.

Purpose: Track recurring home appliance usage that significantly impacts CO2 footprint.
Output: Weekly survey UI, appliance CO2 factors, scheduler notification, footprint integration.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@scheduler/app/main.py
@footprint-engine/app/factors.py
@actions-engine/app/service.py
@frontend/src/pages/Dashboard.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create weekly survey backend and appliance CO2 factors</name>
  <files>actions-engine/app/schemas.py, actions-engine/app/service.py, actions-engine/app/router.py, footprint-engine/app/factors.py</files>
  <action>
  **footprint-engine/app/factors.py** — Add appliance emission factors:
  - WASHING_MACHINE_PER_CYCLE_KG: cold(30°)=0.3, warm(40°)=0.6, hot(60°)=1.0, very_hot(90°)=1.5
    (Source: EU Energy Label data, average Italian grid mix 0.25 kg CO2/kWh)
  - DISHWASHER_PER_CYCLE_KG: eco=0.4, normal=0.7, intensive=1.1
  - DRYER_PER_CYCLE_KG: 2.0 (if applicable)

  **actions-engine/app/schemas.py** — Add:
  - WeeklySurveyRequest:
    - washing_machine_cycles: int (0-14)
    - washing_machine_temp: enum (cold, warm, hot, very_hot)
    - dishwasher_cycles: int (0-14)
    - dishwasher_mode: enum (eco, normal, intensive)
    - week_start: date
  - WeeklySurveyResponse: survey data + calculated co2_kg breakdown

  **actions-engine/app/service.py:**
  - submit_weekly_survey(user_id, request) → calculate CO2 using factors, create ActionLog with type=appliance
  - get_latest_survey(user_id) → most recent survey

  **actions-engine/app/router.py:**
  - POST /api/v1/actions/weekly-survey → submit survey
  - GET /api/v1/actions/weekly-survey/latest → get latest
  </action>
  <verify>cd actions-engine && python -c "from app.schemas import WeeklySurveyRequest; print('OK')"</verify>
  <done>Weekly survey endpoint accepts appliance usage data and calculates CO2 per cycle. Stored as ActionLog.</done>
</task>

<task type="auto">
  <name>Task 2: Build weekly survey UI and integrate with dashboard</name>
  <files>frontend/src/pages/WeeklySurvey.jsx, frontend/src/api/actions.js, frontend/src/pages/Dashboard.jsx, frontend/src/App.jsx, api-gateway/app/router.py</files>
  <action>
  **WeeklySurvey.jsx:**
  - Card-based form, Italian UI:
    - "🧺 Lavatrice" section:
      - Cycle count selector (0-14, stepper)
      - Temperature selector (Freddo 30°, Tiepido 40°, Caldo 60°, Molto caldo 90°)
    - "🍽 Lavastoviglie" section:
      - Cycle count selector (0-14, stepper)
      - Mode selector (Eco, Normale, Intensivo)
  - Submit button → POST /api/v1/actions/weekly-survey
  - Show calculated CO2 result with breakdown
  - "Questa settimana: X kg CO2 per gli elettrodomestici"

  **actions.js:** Add submitWeeklySurvey(), getLatestSurvey()

  **Dashboard.jsx:**
  - Add a prompt card: "Compila il questionario settimanale" if no survey this week
  - Show last survey results in a small card if submitted

  **App.jsx:** Add route /weekly-survey

  **api-gateway/app/router.py:** Add proxy routes for weekly-survey endpoints to actions-engine.
  </action>
  <verify>WeeklySurvey.jsx file exists with form inputs for both appliances</verify>
  <done>Weekly survey page with lavatrice + lavastoviglie inputs. Dashboard shows survey prompt/results. API gateway proxies survey endpoints.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Weekly survey form collects washing machine and dishwasher data
- [ ] CO2 calculated per cycle based on temperature/mode
- [ ] Survey stored as action log entry
- [ ] Dashboard prompts for weekly survey if not done
- [ ] API gateway routes survey requests correctly
</verification>

<success_criteria>

- All tasks completed
- Weekly survey captures appliance usage
- CO2 accurately calculated using EU energy factors
- Dashboard integrates survey prompt and results
</success_criteria>

<output>
After completion, create `.planning/phases/01-green-buddy-ux/01-05-SUMMARY.md`
</output>
